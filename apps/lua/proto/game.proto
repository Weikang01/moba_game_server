syntax="proto3";

enum Stype {
	INVALID_STYPE = 0;
	eAuth         = 1;
	eSystem       = 2;
	eLogic        = 3;
}

enum Cmd {
	INVALID_CMD      = 0;
	eGuestLoginReq   = 1;
}

message GuestLoginReq {
	string guest_key = 1;
}
